package nl.itopia.modwillie.data.data;

/**
 * An enum that is used with the Pattern. This makes it easier to lookup the per used defined patterns
 * @author Robin de Jong
 */
public enum NotificationType {
	INCOMMING, VIBRATION, VIBRARTION_CONT;
}
